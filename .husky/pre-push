protected_branch='master'
current_branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$current_branch" = "$protected_branch" ]; then
  echo "husky - direct push to '$protected_branch' blocked. Open a PR instead."
  exit 1
fi
